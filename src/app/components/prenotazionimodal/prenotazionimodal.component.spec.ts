import { ErrormodalComponent } from './prenotazionmodal.component';

describe('ErrormodalComponent', () => {
  let component: ErrormodalComponent;

  beforeEach(() => {
    component = new ErrormodalComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
