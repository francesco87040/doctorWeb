import { HeaderModule } from './header.module';

describe('HeaderModule', () => {
  const module: HeaderModule = new HeaderModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
